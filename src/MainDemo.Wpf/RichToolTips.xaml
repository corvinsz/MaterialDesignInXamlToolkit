<UserControl x:Class="MaterialDesignDemo.RichToolTips"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:MaterialDesignDemo.Shared.Converters;assembly=MaterialDesignDemo.Shared"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MaterialDesignDemo"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:smtx="clr-namespace:ShowMeTheXAML;assembly=ShowMeTheXAML"
             xmlns:wpf="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
  <UserControl.Resources>
    <ResourceDictionary>
      <!-- This is needed to avoid runtime exceptions?! Seems like this might be a bug? -->
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.RichToolTip.xaml" />
      </ResourceDictionary.MergedDictionaries>
      <Thickness x:Key="Spacer">0,16,0,0</Thickness>
    </ResourceDictionary>

  </UserControl.Resources>
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="250" />
    </Grid.ColumnDefinitions>

    <Grid Grid.Column="0">

      <smtx:XamlDisplay HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        UniqueKey="richToolTip_1">
        <materialDesign:RichToolTip IsPopupOpen="{Binding ElementName=tbIsPopupOpen, Path=IsChecked}"
                                    PlacementMode="{Binding ElementName=RichToolTipPlacementMode, Path=SelectedItem, Mode=TwoWay}"
                                    PopupElevation="{Binding ElementName=ElevationComboBox, Path=SelectedItem, Mode=TwoWay}"
                                    PopupHorizontalOffset="{Binding ElementName=PopupHorizontalOffset, Path=Value, Mode=TwoWay}"
                                    PopupUniformCornerRadius="{Binding ElementName=UniformCornerRadiusComboBox, Path=Value, Mode=TwoWay}"
                                    PopupVerticalOffset="{Binding ElementName=PopupVerticalOffset, Path=Value, Mode=TwoWay}"
                                    Style="{StaticResource MaterialDesignRichToolTip}">
          <materialDesign:RichToolTip.PopupContent>
            <Grid Width="400">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>

              <TextBlock Grid.Row="0"
                         Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                         Text="Rich tooltip" />
              <TextBlock Grid.Row="1"
                         Text="Rich tooltips bring attention to a particular element of feature that warrants the user's focus. It supports multiple lines of informational text."
                         TextWrapping="Wrap" />

              <StackPanel Grid.Row="2" Orientation="Horizontal">
                <Button Content="Action 1" Style="{StaticResource MaterialDesignFlatButton}" />
                <Button Margin="4,0,0,0"
                        Content="Action 2"
                        Style="{StaticResource MaterialDesignFlatButton}" />
              </StackPanel>

            </Grid>
          </materialDesign:RichToolTip.PopupContent>
        </materialDesign:RichToolTip>
      </smtx:XamlDisplay>


      <!--<materialDesign:PopupBox
                               PlacementMode="{Binding ElementName=PopupBoxPlacementModeComboBox, Path=SelectedItem}"
                               PopupElevation="{Binding ElementName=ElevationComboBox, Path=SelectedItem}"
                               PopupHorizontalOffset="{Binding ElementName=HorizontalOffsetComboBox, Path=SelectedItem}"
                               PopupUniformCornerRadius="{Binding ElementName=UniformCornerRadiusComboBox, Path=SelectedItem}"
                               PopupVerticalOffset="{Binding ElementName=VerticalOffsetComboBox, Path=SelectedItem}"
                               SnapsToDevicePixels="True"
                               Style="{Binding ElementName=StyleComboBox, Path=SelectedItem, Converter={StaticResource ComboBoxItemToStyleConverter}}">
        <ContentControl ContentTemplate="{Binding ElementName=ContentComboBox, Path=SelectedItem, Converter={StaticResource ComboBoxItemToDataTemplateConverter}}" />
      </materialDesign:PopupBox>-->
    </Grid>

    <StackPanel Grid.Column="1">
      <GroupBox Padding="8" Header="Properties">
        <StackPanel Orientation="Vertical">

          <DockPanel>
            <ToggleButton x:Name="tbIsPopupOpen"
                          DockPanel.Dock="Right"
                          Style="{StaticResource MaterialDesignSwitchToggleButton}" />
            <TextBlock DockPanel.Dock="Left" Text="IsPopupOpen" />
          </DockPanel>

          <ComboBox x:Name="ElevationComboBox"
                    Margin="{StaticResource Spacer}"
                    materialDesign:HintAssist.Hint="Elevation"
                    materialDesign:TextFieldAssist.HasLeadingIcon="True"
                    materialDesign:TextFieldAssist.LeadingIcon="Layers"
                    SelectedIndex="6"
                    Style="{StaticResource MaterialDesignFilledComboBox}" />

          <materialDesign:NumericUpDown x:Name="UniformCornerRadiusComboBox"
                                        Margin="{StaticResource Spacer}"
                                        materialDesign:HintAssist.Hint="UniformCornerRadius"
                                        materialDesign:TextFieldAssist.HasLeadingIcon="True"
                                        materialDesign:TextFieldAssist.LeadingIcon="BorderRadius"
                                        Minimum="0"
                                        Style="{StaticResource MaterialDesignFilledNumericUpDown}"
                                        Value="8" />

          <materialDesign:NumericUpDown x:Name="PopupHorizontalOffset"
                                        Margin="{StaticResource Spacer}"
                                        materialDesign:HintAssist.Hint="PopupHorizontalOffset"
                                        materialDesign:TextFieldAssist.HasLeadingIcon="True"
                                        materialDesign:TextFieldAssist.LeadingIcon="CompareHorizontal"
                                        Style="{StaticResource MaterialDesignFilledNumericUpDown}"
                                        Value="0" />

          <materialDesign:NumericUpDown x:Name="PopupVerticalOffset"
                                        Margin="{StaticResource Spacer}"
                                        materialDesign:HintAssist.Hint="PopupVerticalOffset"
                                        materialDesign:TextFieldAssist.HasLeadingIcon="True"
                                        materialDesign:TextFieldAssist.LeadingIcon="CompareVertical"
                                        Style="{StaticResource MaterialDesignFilledNumericUpDown}"
                                        Value="0" />

          <ComboBox x:Name="RichToolTipPlacementMode"
                    Margin="{StaticResource Spacer}"
                    materialDesign:HintAssist.Hint="RichToolTipPlacementMode"
                    SelectedIndex="1"
                    Style="{StaticResource MaterialDesignFilledComboBox}" />
        </StackPanel>
      </GroupBox>
    </StackPanel>
  </Grid>
</UserControl>

